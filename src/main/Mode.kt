package main

/**
 * the `Display` and `Control Mode`<br></br>
 * LAYER, IMAGE, PAINT, LOADING
 * @author McAJBen@gmail.com
 * @since 1.6
 */
enum class Mode {
    LAYER,
    IMAGE,
    PAINT,
    LOADING
}