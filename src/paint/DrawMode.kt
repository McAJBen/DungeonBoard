package paint

/**
 * the `DrawMode` for a touch pad in `DrawPanel`
 * ANY, VISIBLE, INVISIBLE, WINDOW
 * @author McAJBen@gmail.com
 * @since 1.6
 */
enum class DrawMode {
    ANY,
    VISIBLE,
    INVISIBLE,
    WINDOW
}